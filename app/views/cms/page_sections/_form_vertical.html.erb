<%= form_for(@cms_page_section) do |f| %>
  <% if @cms_page_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cms_page_section.errors.count, "error") %> prohibited this cms_page_section from being saved:</h2>

      <ul>
      <% @cms_page_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
	  <%= f.hidden_field :page_id, :value => @cms_page_section.page_id, :class => "form-control" %>
  </div>

   <div class="form-group">
    <%= f.label :photo %><br>
    <%= f.file_field :photo, :class => "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :body %><br>
    <%= f.text_area :body, :rows => 10, :class => "tinymce form-control" %>
    <%= tinymce %>
  </div>

  <div class="form-group">
    <%= f.submit %>
  </div>
<% end %>
