<%= form_for(@cms_role) do |f| %>
  <% if @cms_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cms_role.errors.count, "error") %> prohibited this cms_role from being saved:</h2>

      <ul>
      <% @cms_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
