<%= form_for(@cms_attachment) do |f| %>
  <% if @cms_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cms_attachment.errors.count, "error") %> prohibited this cms_attachment from being saved:</h2>

      <ul>
      <% @cms_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :document %><br>
    <%= f.text_field :document %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
