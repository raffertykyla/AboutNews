<%= form_for(@cms_chapter_meeting_minute) do |f| %>
	<% if @cms_chapter_meeting_minute.errors.any? %>
		<div id="error_explanation">
			<h2><%= pluralize(@cms_chapter_meeting_minute.errors.count, "error") %> prohibited this cms_chapter_meeting_minute from being saved:</h2>
			<ul>
				<% @cms_chapter_meeting_minute.errors.full_messages.each do |message| %>
					<li><%= message %></li>
				<% end %>
			</ul>
		</div>
	<% end %>
	<div class="field">
		<%= f.label :title %><br>
		<%= f.text_field :title %>
	</div>
	<div class="field">
		<%= f.label :meeting_date %><br>
		<%= f.date_select :meeting_date %>
	</div>
	<div class="field">
		<%= f.label :document %><br>
		<%= f.file_field :document, class: "form-control" %>
	</div>
	<div class="field">
		<%= f.label :chapter_id %><br>
		<%= f.select(:chapter_id, Cms::Chapter.all.collect {|a| [a.name, a.id]}) -%>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>